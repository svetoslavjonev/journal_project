# Copy this file to local_settings.py for machine-specific development overrides.

# DEBUG = True
# ALLOWED_HOSTS = ['127.0.0.1', 'localhost']
# CSRF_TRUSTED_ORIGINS = []

# Local-only overrides belong here. Production configuration should use
# environment variables instead of committing secrets or hostnames.
